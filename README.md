# theme-memories/theme-memories

Astro framework blog theme

## About src/content folder

This folder is served in a private submodule repo, so the content itself is not presented publicly unless chosen to.  
Always commit and push this submodule first, before commit the main repo.  
Please use `pnpm prettier --write --ignore-unknown src/content` to manual sanitize format before commit.
